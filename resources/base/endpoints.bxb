endpoints {
  action-endpoints {
    action-endpoint (SendLetter) {
      accepted-inputs (sendLetterRequest)
      local-endpoint ("SendLetter.js")
    }
    action-endpoint (FindCountry) {
      accepted-inputs (searchCountry)
      local-endpoint ("FindCountry.js")
    }
  }
}
